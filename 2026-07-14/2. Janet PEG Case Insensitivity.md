
```
(defn ci [x]
    ['* ;(map (fn [b]
               (cond (and (< 64 b) (< b 91))  ['set (string/format "%c%c" b (+ b 32))]
                     (and (< 96 b) (< b 123)) ['set (string/format "%c%c" (- b 32) b)]
                     (string/format "%c" b))) x)])

(def grammar
    ~{:startfile  (* ,(ci "StartFile") "," (<- (some :w)))
      :startcount (* ,(ci "StartCount") "," (number (some :d)))
      :command (* ,(ci "$GORK,Xmd") "," (+ :startfile :startcount))
      :main :command
    })

> (peg/match grammar "$GOrk,xmd,startfile,nackleFRACKLE")
@["nackleFRACKLE"]
```

Why didn't you write something like
```
(peg/match grammar (string-concat (to-lower-case "$GOrk,xmd,startfile,") "nackleFRACKLE"))
```
?

or, simply insist that "$gork,xmd" and "StartFile" and "StartCount" be spelled in only one way,
```
(peg/match grammar "$gork,xmd,StartFile,nackleFRACKLE")
```
