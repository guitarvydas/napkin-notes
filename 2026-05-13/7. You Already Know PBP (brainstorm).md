> Every Unix pipe is a PBP component.
...
> The Container — the shell ...

I'm not sure about this wording. 

A Unix pipeline is a Container.

A command in a pipeline is a Part. Maybe a Leaf or maybe another Container (pipeline).

A sequential pipeline is a kind of Container, but, a Container is more general than a sequential pipeline. 

> Where Pipes Stop

I like this whole section.
