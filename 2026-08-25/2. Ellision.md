I want to elide the full code version, to show only the grammar part, e.g.

```rebol
parse "(416)555-1234" [
    "(" copy area 3 digits ")"
    copy exch 3 digits "-"
    copy line 4 digits
    (print rejoin [area "-" exch "-" line])
]
```

can be displayed as:

```rebol
parse "(416)555-1234" [
    "(" 3 digits ")"
    3 digits 
    "-"
    4 digits
]
```
