The short version contains this paragraph:

> That's fine, but every rewrite rule turns into a little blob of string concatenation and escaping. Not hard, just noise.

The problem is that we usually can't see the forest for the trees. Rewriting text is simple, but we're tempted to use all of the overkill available in general purpose languages.

[address only that issue by rewriting that paragraph(s), no need to re-display the whole thing]