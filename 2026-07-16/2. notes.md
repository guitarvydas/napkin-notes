make the 3 points into distinct markdown points in the second paragraph that begins with "I recently build a little DSL...".

---

> That's fine, but every rewrite rule turns into a little blob of string concatenation and escaping. Not hard, just noise. 

The point here is that we've overcomplicated our own problems by trying to generalize and trying to produce general purpose languages. By thinking about only what is needed to successfully generate SCNs, I discovered that I needed only to use strings and simple string operations. These operations are a subset of what's available in most modern programming languages, so it was like cherry picking - choose the minimal subset of what would be useful and ignore the rest of the complications that have been added to programming languages.

---
include an appendix with
reference to T2T - T2T is one of the tools in the PBP toolkit, once you install PBP, you have access to T2T

--- 

use distinct markdown points to list the 2 kinds of code in the paragraph that begins with "Code like this ..."

---
>Bad way to build a shipped product. Exactly the right way to build a tool for myself, this week, for this problem. 

The tool was, indeed, built quickly, but I've been fruitfully using it for years.
