
---

> [!done]
> One typo to flag: "The reasoning guarantees the notation had offered began to erode" — dropped a word in the re-assignment paragraph. Should read "The reasoning guarantees _that_ the notation had offered began to erode" — or better, restructured as "The guarantees the notation had offered began to erode." You'll want to fix that manually.

Otherwise the main change from draft 1 is the sentence rhythm throughout — shorter, flatter, less subordination. What wants changing?


---
I like "The guarantees the notation had offered began to erode." best and have made the change.

> The functional paradigm has a precise implicit target: classical physics.

I'm not sure that this captures what I am thinking. SICP's target is a style of thinking that neglects other possibilities. This style has been used to describe classical physics - SICM. Prigogene argues against using this style exclusively. Paul LaViolette (subquantum kinetics) notes that this style is based on thinking in terms of closed systems and he favours using an open system approach. 

I "feel" this difference when I think of programming a single CPU in assembler - it's sequential and synchronous. Yet, when I think in terms of using multiple CPUs, each CPU being just a simple IC - just a "building material" - I end up thinking in terms of asynchronous parallelism where many things can happen at the same time without synchronizing with one another. Each CPU is a closed system which operates in a sequential / synchronous manners, but the whole system composed of many CPUs is not sequential and synchronous and is "open".

The "ultimate" use for SICP is systems like SICM. This requires operating systems and the functional paradigm as scaffolding. Ideas like Forth lead to a more "open systems" approach which does not constrain one to arrive at functional, closed system expressions of physics nor Reprogrammable Machines.

The way I see it, the functional paradigm - SICP, SICM - is but a subset of a broader class of expression. Presently, I think that the broader class cannot be expressed using only one notation, but needs to incorporate many specialized, custom notations. This _should_ lead to expression of irreversible processes and _should_ make it easier to explore ideas like the Brusselator, Subquantum Mechanics, etc. 

---
*fact check: Sector Lisp "It runs the proof assistant that shipped with Lisp 1.5."*

The claim is confirmed. SectorLISP can run the proof assistant that was included in LISP 1.5 — that's a direct quote from Justine Tunney's own page at justine.lol. The line in the article stands as written.

---
---

> > [!warning]
The functional paradigm has a precise implicit target: classical physics. In that domain it is powerful and elegant. Equations are timeless. Functions are pure. State does not exist. Time does not pass. The notation fits the domain because the domain was defined to exclude everything the notation cannot handle. That is not a criticism. It is a description of what the notation is for.

I want this to be a list of "features" of the functional domain, followed by the idea that classical physics is based on this paradigm, which then leads to SICM being based on SICP.

Something like:

In the functional domain
- Equations are timeless. 
- Functions are pure. 
- State does not exist. 
- Time does not pass. 

...

---
Here's a restructured version. Note this is a formatting exception from your usual style — worth a conscious decision rather than an accident:

---

The functional domain has a precise set of properties.

- Equations are timeless.
- Functions are pure.
- State does not exist.
- Time does not pass.
- Order is irrelevant.

Classical physics is built on exactly these properties. It was defined that way. SICP provides a notation for reasoning within these constraints. SICM uses that notation to express classical mechanics directly. The fit is exact because the domain and the notation share the same assumptions. That is not a criticism. It is a description of what the notation is for.

---

Two things to decide: whether "Order is irrelevant" belongs in the list — I added it because it connects to your sequentialism argument later, but drop it if it feels like a stretch. And whether you want the list as bullets or as a run of short declarative sentences with line breaks between them, which preserves the rhetorical staccato without the bullet formatting.

---
---

Yes, let's use a run of short declarative sentences. Followed by a paragraph that emphasize something like this: This is not how computers work. This is not how the internet works. This is the way that a single part (the CPU) of a computer works and a single node in the internet works.

---

Here's that section:

---

The functional domain has a precise set of properties.

Equations are timeless. Functions are pure. State does not exist. Time does not pass. Order is irrelevant.

Classical physics is built on exactly these properties. SICP provides a notation for reasoning within these constraints. SICM uses that notation to express classical mechanics directly. The fit is exact because the domain and the notation share the same assumptions.

This is not how computers work. This is not how the internet works. This is how a single CPU works. This is how a single node in a network works. The moment you connect two CPUs, or two nodes, time reappears. State reappears. Order matters again. The functional paradigm describes one part of the machine accurately and says nothing useful about what happens between parts.

---

That last sentence is doing a lot of work — it sets up the "wrong paradigm for the whole system" argument cleanly. Does it land the way you want, or do you want it harder?