The following is on the same track, but feels like a different article:

I see SICP as scaffolding for SICM.

I see "operating systems" as scaffolding for SICP.

To me this isn't generalized "programming". It is customized "programming". It solves one kind of problem well, but doesn't generalize to other problems (like state-based and control flow problems).

[research: how big was Sketchpad, in bytes? how big was Unix V1? TurboPascal? Lisp 1.5? Visicalc?] When we built useful programs in assembler, they had fairly small footprints. Today, we need to speak in terms of megabytes instead of in kilobytes. This, itself, should be a _tell_ that force-fitting the sequential / synchronous paradigm onto modern problems is causing waste.

- need ref to related article "Painting Ourselves Into A Corner"
