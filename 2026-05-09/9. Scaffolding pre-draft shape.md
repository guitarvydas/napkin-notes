Before drafting, let me make sure I have the shape of it. A few questions to focus the brainstorm:

The SICP/SICM/OS scaffolding chain — what's the claim exactly? That each layer is solving a narrow problem well, and we mistake that for general-purpose programming? Or that the layers are accumulating and each one adds cost without adding expressibility?

The memory footprint argument — is the claim that bloat is _caused_ by force-fitting sync/sequential onto problems it doesn't fit? Or is it the broader point that we've stopped measuring cost against what actually worked in the past?

And is the type-checking point (3% of errors, Sketchpad/Unix/VisiCalc built without it) part of this article, or still its own piece?

---

