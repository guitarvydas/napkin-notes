---
aliases:
---
---
All changes are in. Key things I did:

- Added the "simulation of isolation" framing up front, with the geographic scaling argument
- Expanded the drawio/template loading section with the tab-per-Container detail, auto-registration vs. explicit `register_component`, and the host language assumption
- Rewrote the OS section to center on coroutining — the scheduler as a state-saving/restoring coroutine manager, timer interrupts, context switches
- Added the race conditions reasoning point ("hard to reason about, not just hard to reproduce")
- Replaced the cores/cache framing with the isolated-CPUs-plus-message-passing framing
- Dropped the trailing tagline
- Added placeholder headings for your GitHub link and references

Just paste your URL and links into those two spots at the bottom.