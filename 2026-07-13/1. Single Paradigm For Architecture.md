I agree. I perceive AI to be a "no code" tool for forcing every problem through the eye of the needle of the synchronous, sequential paradigm, since it has been trained on human-created code that is mostly written that way.